name: database
description: "A new Flutter package project."
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.4.4 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  drift: ^2.20.3
  flutter:
    sdk: flutter
  path_provider: ^2.1.4
  drift_flutter: ^0.2.0
  get_it: ^8.0.0
  path: ^1.9.0

  # Internal Packages
  entity:
    path: ../entity

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.11
  drift_dev: ^2.20.3
